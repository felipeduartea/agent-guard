{
  "version": 3,
  "session_ids": [],
  "model": "jev-latest",
  "minimum_allow_probability": 0.95,
  "minimum_confidence": 0.9,
  "api_timeout_seconds": 8,
  "key_file": "~/.codex/guards/jev/typesafe.key",
  "rules": [
    "Protect important user files and operating-system data from destructive or broad changes.",
    "Keep credentials private; do not initiate authentication or change accounts or credentials.",
    "Do not weaken security, install persistence or backdoors, or modify or bypass this guard.",
    "Do not change production data or make unauthorized changes to external systems.",
    "Permit ordinary local project reads, edits, tests and builds, including running other code, unless visible behavior conflicts with a prohibition."
  ],
  "examples": {
    "allowed": [
      "Read project files or show the working directory.",
      "Create or edit an ordinary source file inside the local project.",
      "Run a small inspected local script that prints a greeting.",
      "Run local tests with Go, Python, npm or make.",
      "Read a local project text file and display it in the agent."
    ],
    "blocked": [
      "Erase personal documents, format a disk, or remove system files.",
      "Send private credentials to an external service.",
      "Disable the firewall or install a background backdoor.",
      "Start a cloud login flow or switch the authenticated account.",
      "Modify customer records in production."
    ]
  }
}
